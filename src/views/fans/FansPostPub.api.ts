import { defHttp } from '/@/utils/http/axios';
import { useMessage } from "/@/hooks/web/useMessage";

const { createConfirm } = useMessage();

enum Api {
  list = '/content/fansPostPub/list',
  save='/content/fansPostPub/add',
  edit='/content/fansPostPub/edit',
  deleteOne = '/content/fansPostPub/delete',
  deleteBatch = '/content/fansPostPub/deleteBatch',
  importExcel = '/content/fansPostPub/importExcel',
  exportXls = '/content/fansPostPub/exportXls',
  publishOne= '/content/fansPostPub/publishOne',
}

/**
 * 导出api
 * @param params
 */
export const getExportUrl = Api.exportXls;

/**
 * 导入api
 */
export const getImportUrl = Api.importExcel;

/**
 * 列表接口
 * @param params
 */
export const list = (params) => defHttp.get({ url: Api.list, params });

/**
 * 删除单个
 * @param params
 * @param handleSuccess
 */
export const deleteOne = (params,handleSuccess) => {
  return defHttp.delete({url: Api.deleteOne, params}, {joinParamsToUrl: true}).then(() => {
    handleSuccess();
  });
}
export const publishOne = (params,handleSuccess,handleFail) => {
  return defHttp.post({url: Api.publishOne, params}, {joinParamsToUrl: true}).then(() => {
    handleSuccess();
  }).catch((err) => {
    handleFail();
  });;
}

/**
 * 批量删除
 * @param params
 * @param handleSuccess
 */
export const batchDelete = (params, handleSuccess) => {
  createConfirm({
    iconType: 'warning',
    title: '确认删除',
    content: '是否删除选中数据',
    okText: '确认',
    cancelText: '取消',
    onOk: () => {
      return defHttp.delete({url: Api.deleteBatch, data: params}, {joinParamsToUrl: true}).then(() => {
        handleSuccess();
      });
    }
  });
}

/**
 * 保存或者更新
 * @param params
 * @param isUpdate
 */
export const saveOrUpdate = (params, isUpdate) => {
  let url = isUpdate ? Api.edit : Api.save;
  return defHttp.post({ url: url, params }, { isTransformResponse: false });
}
